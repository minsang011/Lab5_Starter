1. Because thoughtful naming can improve code readability, maintainability, and efficiency in targeting elements for styling and scripting.
2. Data attributes are custom attributes that store additional information about an element that doesn't have any visual representation.
3. A DOM fragment is a document that has no parent.
4. The Virtual DOM is a concept employed by libraries like React to improve frontend performance.
5. We can utiilze document.querySelector to choose the desired attributes in JS.
6. addEventListner can do more flexible operations in JS, while onClick is more HTML attribute which performs like a simple addEventListner in JS.